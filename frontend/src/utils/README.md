# src/utils

Pure TypeScript helper functions with no React dependencies.

Examples:
- `formatters.ts` — number/date/percentage formatting for display
- `statistics.ts` — client-side helpers for rendering confidence intervals, p-values
- `colors.ts` — Experiment status → Tailwind color class mapping

Every function should be easily unit-testable (no DOM, no network).
